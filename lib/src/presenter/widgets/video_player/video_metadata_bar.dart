import 'package:flutter/widgets.dart';

class VideoMetadataBar extends StatelessWidget {
  const VideoMetadataBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
